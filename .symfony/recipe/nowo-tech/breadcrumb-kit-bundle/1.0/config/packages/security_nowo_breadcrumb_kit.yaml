# Merged into security.access_control by Symfony (REQ-SEC-004).
# Adjust path if you changed nowo_breadcrumb_kit.dashboard.path_prefix.
security:
    access_control:
        - { path: ^/breadcrumb-kit-admin, roles: ROLE_ADMIN }
